<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hwadee.hello.mapper.AccountMapper">
	
	<select id="findById" parameterType="int" resultType="com.hwadee.hello.domain.Account">
		SELECT * FROM ACCOUNT WHERE ACCOUNT_ID = #{id}
	</select>
 
</mapper>